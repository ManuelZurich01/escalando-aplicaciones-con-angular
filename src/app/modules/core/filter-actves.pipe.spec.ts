import { FilterActvesPipe } from './filter-actves.pipe';

describe('FilterActvesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterActvesPipe();
    expect(pipe).toBeTruthy();
  });
});
